执行脚本的时候，可以指令后面添加参数，如
./test.sh 1 2 3

相当于传入了三个参数

在脚本内可以通过下列符号表达对应的内容
$0 代表执行文件的名称
$# 代表传到到脚本的参数个数
$* 以字符串的形式显示所有传入的参数 带引号时 “$1 $2 ...” 
$$ 脚本运行的当前进程ID
$! 后台运行的最后一个进程的ID号
$@ 与$*类似，但是每个参数以引号隔开了 带引号时“$1”“$2”..
$- 显示shell使用的当前选项，与set命令功能相同
$? 显示最后命令的推出状态，0表示没有粗欧文，其他任何值均表明有错误

$1 代表第一个参数
一次类推